const assert = require('assert');
const { Given, When, Then } = require('cucumber');

Given('Gerar um código para layout de vídeo', function () {
    // Ações concretas aqui.
    return 'pending';
  });
  When('O código não é válido', function () {
     // Ações concretas aqui.
    return 'pending';
  });
  Then('Eu recebo a notificação {string}', function (string) {
     // Ações concretas aqui.
    return 'pending';
  });